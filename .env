# ============================================================
# Variables de entorno del proyecto
# ============================================================
# Copia este archivo como ".env" (sin el ".example") en la raíz
# del proyecto, junto a manage.py, y rellena los valores reales.
#
# IMPORTANTE: el archivo ".env" real (con tus credenciales de verdad)
# NUNCA debe subirse a git. Este archivo ".env.example" sí se puede
# subir, porque no tiene datos sensibles, solo sirve de plantilla.

EMAIL_HOST_USER=stails290@gmail.com
EMAIL_HOST_PASSWORD=wdhb ajkh mhno xdot 
DOMINIO_SITIO=http://127.0.0.1:8000
python3 -c "import smtplib; s=smtplib.SMTP('smtp.gmail.com',587); s.starttls(); s.login('stails290@gmail.com','wdhb ajkh mhno xdot'); print('Login OK'); s.quit()"